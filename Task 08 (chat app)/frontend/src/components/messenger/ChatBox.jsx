import { Box } from "@mui/material";
import ChatHeader from "../navbars/ChatHeader";

const ChatBox = () => {
  return (
    <Box sx={{ height: "75%" }}>
      <ChatHeader />
    </Box>
  );
};

export default ChatBox;
